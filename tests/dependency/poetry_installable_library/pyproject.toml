[tool.poetry]
authors = ["Substra dev team"]
description = "Test packages for substrafl."
name = "substrafltestlibrary"
version = "0.1.0"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry_core>=1.0.0"]
